# Tmux
if command -v tmux >/dev/null
 then
     alias ta='tmux attach -t ' 
 fi

# Eza
if command -v eza >/dev/null
 then
     alias ls=eza
     alias ll='eza -lg'
     alias tree='eza -T'
 fi

# bat
if command -v bat >/dev/null
 then
    alias cat=bat
 fi
